<% content_for :page_title, t('pricing_rules.edit') %>

<div class="p-8">
  <div class="max-w-3xl mx-auto">
    <div class="mb-8">
      <h1 class="text-3xl font-bold"><%= t('pricing_rules.edit') %></h1>
      <p class="mt-2 text-gray-600"><%= @pricing_rule.vehicle_type_name %></p>
    </div>

    <div class="bg-white shadow rounded-lg p-6">
      <%= render 'form', pricing_rule: @pricing_rule %>
    </div>
  </div>
</div>
